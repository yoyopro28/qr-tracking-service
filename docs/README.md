# Documentation index

The current runtime architecture and deployment contract are documented in
[architecture.md](architecture.md), the ordered
[production deployment runbook](production-deployment.md), and the repository
root [README](../README.md).

The remaining `*-notes.md`, Prisma reviews, implementation plans and
`zwischenstand.md` describe the historical Node/Next.js/PostgreSQL/filesystem MVP.
They are retained as decision history only and are not operational instructions
for the current branch. The exact historical application is available from the
immutable `node-postgres-filesystem-v1` tag.
