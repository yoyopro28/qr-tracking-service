version: "3.9"

services:
  postgres:
    image: postgres:16
    container_name: qr-tracking-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: qr_tracking_service
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
